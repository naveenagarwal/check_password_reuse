require 'spec_helper'

describe CheckPasswordReuse do
  it 'has a version number' do
    expect(CheckPasswordReuse::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
